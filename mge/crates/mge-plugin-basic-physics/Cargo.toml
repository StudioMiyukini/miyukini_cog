[package]
name = "mge-plugin-basic-physics"
version = "0.1.0"
edition = "2021"
description = "MGE Core Pack — Collider, RigidBody, CollisionEvent"

[dependencies]
mge-ecs = { path = "../mge-ecs" }
mge-event = { path = "../mge-event" }
mge-plugin-spatial = { path = "../mge-plugin-spatial" }

[lints.rust]
unsafe_code = "forbid"
