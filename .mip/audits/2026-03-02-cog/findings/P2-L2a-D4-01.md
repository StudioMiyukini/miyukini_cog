# Finding List P2-L2a-D4-01

Agent: Lise
Domaine: D4 -- UI/UX
Lot: L2 -- Sous-lot: L2a (jayxpose)
Crates: jayxpose-app (apps/jayxpose/)
Fichiers audites: 13/13
Debut: 2026-03-02T14:30:00Z
Fin: 2026-03-02T15:15:00Z

## Findings

| # | Sev | WCAG/ISO Ref | Fichier:Ligne | Probleme | Impact | Fix |
|---|-----|-------------|---------------|----------|--------|-----|
| 1 | MAJOR | WCAG 1.3.1, 3.3.2 | components.rs:198-211 | `FormField` : le `<label>` et l'`<input>` ne sont pas lies par `for`/`id`. L'association est uniquement visuelle, pas programmatique. | Les technologies d'assistance ne peuvent pas identifier quel label correspond a quel champ. Affecte TOUS les formulaires de l'app (entreprise, produit, onboarding, vitrine, documents). | Generer un `id` unique (ex: `use_signal(uuid)` ou compteur) et ajouter `r#for: "{id}"` sur le `<label>` et `id: "{id}"` sur l'`<input>`. |
| 2 | MAJOR | WCAG 1.3.1, 3.3.2 | components.rs:229-248 | `FormTextarea` : meme probleme que #1, le `<label>` et le `<textarea>` ne sont pas lies par `for`/`id`. | Meme impact que #1. Affecte entreprise.rs (description courte/longue, adresses) et onboarding.rs. | Meme fix que #1 : generer un `id` unique, lier `<label>` et `<textarea>`. |
| 3 | MAJOR | WCAG 1.3.1, 3.3.2 | catalogue.rs:59-69 | `<select>` filtre categorie sans `<label>` associe. Le texte "Toutes les categories" est l'option par defaut, pas un label accessible. | Champ de formulaire non identifiable par technologies d'assistance. | Ajouter un `<label>` explicite (peut etre visuellement masque via `clip: rect(0,0,0,0)`) lie par `for`/`id` au `<select>`. |
| 4 | MAJOR | WCAG 1.3.1, 3.3.2 | catalogue.rs:51-57 | `<input>` de recherche sans `<label>` explicite. Le `placeholder` n'est pas un substitut acceptable pour un label accessible. | Le champ de recherche n'est pas identifiable programmatiquement. | Ajouter un `<label>` visuellement masque ou un `aria-label` sur l'input. |
| 5 | MAJOR | WCAG 1.3.1, 3.3.2 | produit_form.rs:122-135 | `<select>` categorie produit : le `<label>` existe (ligne 118-120) mais n'est pas lie par `for`/`id` au `<select>`. | Association label-champ uniquement visuelle, pas programmatique. | Ajouter `for`/`id` entre le `<label>` et le `<select>`. |
| 6 | MAJOR | WCAG 1.3.1, 3.3.2 | produit_form.rs:147-155 | `<select>` disponibilite : meme probleme, `<label>` non lie au `<select>` par `for`/`id`. | Meme impact que #5. | Meme fix que #5. |
| 7 | MAJOR | WCAG 1.3.1, 3.3.2 | produit_form.rs:162-171 | Checkbox "Produit vedette" : le `<label>` (ligne 167) n'est pas lie a l'`<input type="checkbox">` (ligne 163) par `for`/`id`. | Cliquer sur le label ne coche pas la case. Technologies d'assistance ne lient pas les deux. | Ajouter `for`/`id` ou envelopper l'`<input>` dans le `<label>`. |
| 8 | MAJOR | WCAG 1.3.1, 3.3.2 | fiche_publique.rs:134-135 | `<select>` confidentialite par champ : pas de `<label>` explicitement lie par `for`/`id`. Le `<span>` adjacent (ligne 132) fait office de label visuel mais n'est pas un `<label>`. | Les selecteurs de confidentialite (10 champs) ne sont pas accessibles. | Remplacer le `<span>` par un `<label>` avec `for`/`id`. |
| 9 | MAJOR | WCAG 1.3.1, 3.3.2 | documents.rs:224-226 | `<select>` type de document dans UploadDocument : le `<label>` (ligne 223) n'est pas lie par `for`/`id` au `<select>`. | Meme probleme d'association label-champ. | Ajouter `for`/`id`. |
| 10 | MAJOR | ISO 9241-110 (Tolerance aux erreurs) | catalogue.rs:209 | Suppression produit (`on_delete`) executee immediatement sans confirmation. Un clic accidentel detruit des donnees. | Perte de donnees irreversible sans possibilite d'annulation. | Ajouter un dialogue de confirmation ("Etes-vous sur de vouloir supprimer ce produit ?") avant `produit_delete`. |
| 11 | MAJOR | ISO 9241-110 (Tolerance aux erreurs) | catalogue.rs:291-294 | Suppression categorie executee immediatement sans confirmation. | Perte de donnees irreversible. Peut affecter les produits associes a cette categorie. | Ajouter un dialogue de confirmation. |
| 12 | MAJOR | ISO 9241-110 (Tolerance aux erreurs) | produit_form.rs:194-197 | Suppression produit depuis le formulaire d'edition, sans confirmation. | Meme impact que #10. | Ajouter un dialogue de confirmation. |
| 13 | MAJOR | ISO 9241-110 (Tolerance aux erreurs) | documents.rs:141-143 | Suppression document (`on_delete`) executee immediatement sans confirmation. | Perte de document professionnel irreversible. | Ajouter un dialogue de confirmation. |
| 14 | MAJOR | ISO 9241-110 (Tolerance aux erreurs) | vitrine.rs:61-65 | Publication vitrine (`vitrine_status = "publiee"`) sans confirmation. Rend le profil visible publiquement. | Action a consequence publique executee sans avertissement. | Ajouter confirmation "Votre vitrine sera visible publiquement. Continuer ?". |
| 15 | MAJOR | ISO 9241-110 (Tolerance aux erreurs) | documents.rs:362 | Acceptation de partage document executee sans confirmation. | Partage de documents sensibles sans verification deliberee. | Ajouter confirmation avant `partage_update_status("accepte")`. |
| 16 | MINOR | WCAG 4.1.2 | sidebar.rs:117 | `SidebarItem` utilise `<button>` natif (bon), mais manque de `aria-current="page"` sur l'item actif pour indiquer la page courante. | Technologies d'assistance ne peuvent pas identifier la section active dans la navigation. | Ajouter `aria_current: if is_active { "page" } else { "" }` sur le bouton actif. |
| 17 | MINOR | WCAG 4.1.2 | sidebar.rs:24 | `<aside>` n'a pas de `role="navigation"` ou `aria-label` pour identifier la region de navigation. | La region laterale n'est pas identifiee semantiquement. | Ajouter `role: "navigation"` et `aria_label: "Navigation JayXpose"` sur l'`<aside>`. |
| 18 | MINOR | WCAG 4.1.2 | vitrine.rs:92-97 | Les onglets (Parametres/Pages/Preview) n'utilisent pas `role="tablist"`/`role="tab"`/`aria-selected`. | Pattern d'onglets non identifiable par technologies d'assistance. | Ajouter ARIA roles sur le conteneur d'onglets et chaque `TabButton`. |
| 19 | MINOR | WCAG 1.1.1 | components.rs:29-31 | Icone emoji dans `StatCard` sans texte alternatif (`aria-label`). L'emoji est purement decoratif si le `label` est present, mais devrait porter `aria-hidden: "true"`. | Redondance ou confusion pour les lecteurs d'ecran qui lisent l'emoji. | Ajouter `aria_hidden: "true"` sur le `<span>` contenant l'emoji icone. |
| 20 | MINOR | WCAG 1.1.1 | onboarding.rs:149 | Avatar Miou ("fleur") decoratif sans `aria-hidden`. | Lecteur d'ecran annonce "cherry blossom" inutilement. | Ajouter `aria_hidden: "true"` sur le `<div>` avatar. |
| 21 | MINOR | ISO 9241-110 (Conformite aux attentes) | documents.rs:141 | Le bouton "Voir" un document (DocumentRow) est un TODO vide (`// TODO: ouvrir le document`). L'utilisateur clique sans resultat. | Frustration utilisateur, bouton non fonctionnel. | Implementer l'ouverture du document ou masquer le bouton "Voir" tant que la fonctionnalite n'est pas prete. |
| 22 | MINOR | ISO 9241-110 (Feedback) | catalogue.rs:118-123 | Suppression produit : aucun feedback visuel apres suppression reussie ou echouee (le `let _ = db_del.produit_delete(...)` ignore l'erreur). | L'utilisateur ne sait pas si la suppression a reussi. | Afficher un message de confirmation/erreur apres suppression, et ne pas ignorer le `Result`. |
| 23 | MINOR | ISO 9241-110 (Feedback) | catalogue.rs:291-293 | Suppression categorie : meme probleme, `let _ =` ignore le resultat, pas de feedback. | Meme impact que #22. | Meme fix. |
| 24 | MINOR | ISO 9241-110 (Feedback) | documents.rs:142 | Suppression document : `let _ =` ignore le resultat, pas de feedback. | Meme impact. | Meme fix. |
| 25 | MINOR | WCAG 2.4.7 | components.rs:63 | `ActionButton` et autres boutons n'ont pas de style `:focus-visible` explicite. Dioxus desktop (WebView) applique un focus par defaut, mais le rendu depend du backend. | Focus potentiellement invisible si le WebView supprime l'outline par defaut. | Ajouter un style `outline` explicite sur focus (ex: `outline: 2px solid {accent_blue}`) ou verifier que le WebView preserve le focus natif. |
| 26 | MINOR | RSX piege | documents.rs:60 | `view.set(if *view.read() == DocView::Partages { ... } else { ... })` -- read et set sur le meme signal dans la meme expression. Peut provoquer un panic runtime avec le borrow checker de Signal. | Crash potentiel a l'execution lors du clic sur le bouton "Partages". | Extraire la lecture dans un `let` : `let current = *view.read(); view.set(if current == ... { ... } else { ... });` |
| 27 | INFO | WCAG 1.4.3 | theme (palette) | Les couleurs `text_muted` sur fond `bg_secondary` / `bg_hover` ne sont pas verifiables sans les valeurs exactes de la palette. Risque de ratio de contraste inferieur a 4.5:1 (WCAG AA). | Texte potentiellement illisible pour les utilisateurs malvoyants. | Verifier les ratios de contraste avec les valeurs exactes de `ThemePalette`. Les combos `text_muted`/`bg_secondary` et `text_muted`/`bg_hover` sont les plus a risque. |
| 28 | INFO | ISO 9241-110 (Individualisation) | main.rs:61 | Un seul theme (`Theme::Gaming`) est utilise. Pas de choix utilisateur pour le theme. | Pas de personnalisation de l'apparence. Adequation a l'individualisation minimale. | Futur : proposer un selecteur de theme ou au minimum un mode clair/sombre. Connu sous F-ISO-01. |
| 29 | INFO | Bonne pratique | onboarding.rs | L'onboarding est bien structure en etapes progressives avec Miou. Bon pattern UX pour la premiere utilisation. Pattern de decouverte conforme a ISO 9241-110 (Adequation a la tache). | Positif. | Aucun fix necessaire. |

## Resume

- CRITICAL: 0
- MAJOR: 15
- MINOR: 10
- INFO: 3

### Repartition par referentiel

| Referentiel | CRITICAL | MAJOR | MINOR | INFO |
|-------------|----------|-------|-------|------|
| WCAG 1.1.1 (Contenu non textuel) | 0 | 0 | 2 | 0 |
| WCAG 1.3.1 (Information et relations) | 0 | 9 | 0 | 0 |
| WCAG 1.4.3 (Contraste) | 0 | 0 | 0 | 1 |
| WCAG 2.4.7 (Focus visible) | 0 | 0 | 1 | 0 |
| WCAG 3.3.2 (Labels) | 0 | 9 | 0 | 0 |
| WCAG 4.1.2 (Nom/Role/Valeur) | 0 | 0 | 3 | 0 |
| ISO 9241-110 (Tolerance erreurs) | 0 | 6 | 0 | 0 |
| ISO 9241-110 (Feedback) | 0 | 0 | 3 | 0 |
| ISO 9241-110 (Conformite attentes) | 0 | 0 | 1 | 0 |
| ISO 9241-110 (Individualisation) | 0 | 0 | 0 | 1 |
| RSX piege Dioxus 0.6 | 0 | 0 | 1 | 0 |

Note : les findings #1-#2 (FormField/FormTextarea) sont les plus impactants car ces composants sont utilises dans TOUS les formulaires de l'app (entreprise, produit, onboarding, vitrine, documents). Corriger ces 2 composants resout automatiquement 9 des 15 MAJOR.

### Fichiers les plus impactes

| Fichier | Findings | Severites |
|---------|----------|-----------|
| components.rs | 5 (#1, #2, #19, #25, #29) | 2 MAJOR, 2 MINOR, 1 INFO |
| catalogue.rs | 4 (#3, #4, #10, #11, #22, #23) | 4 MAJOR, 2 MINOR |
| produit_form.rs | 4 (#5, #6, #7, #12) | 4 MAJOR |
| documents.rs | 4 (#9, #13, #15, #21, #24, #26) | 3 MAJOR, 3 MINOR |
| fiche_publique.rs | 1 (#8) | 1 MAJOR |
| sidebar.rs | 2 (#16, #17) | 2 MINOR |
| vitrine.rs | 2 (#14, #18) | 1 MAJOR, 1 MINOR |
| onboarding.rs | 1 (#20) | 1 MINOR |
| main.rs | 1 (#28) | 1 INFO |

### Correctifs prioritaires (impact maximal pour effort minimal)

1. **FormField + FormTextarea** (#1, #2) : Ajouter generation `id` + `for`/`id`. Corrige 9 MAJOR d'un coup.
2. **Dialogue de confirmation generique** (#10-15) : Creer un composant `ConfirmDialog` reutilisable. Corrige 6 MAJOR.
3. **Signal read+set** (#26) : Fix trivial en 1 ligne dans `documents.rs`.

## Erreurs rencontrees

Aucune. Tous les 13 fichiers ont ete lus et analyses avec succes.
