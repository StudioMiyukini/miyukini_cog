# Miyukini Game Engine

Workspace Rust autonome pour `Sodomight`.

## Structure

- `crates/` : briques moteur
- `games/` : executables de jeux

## Commandes locales

```powershell
cargo check --workspace
cargo test --workspace
cargo run -p sodomight
```
