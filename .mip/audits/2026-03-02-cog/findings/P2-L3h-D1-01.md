# Finding List P2-L3h-D1-01
Agent: Francois
Domaine: D1 -- Sante code
Lot: L3 -- Sous-lot: L3h (toolkits social + web/divers)
Crates: miyusocialprofile, miyusocialmessaging, miyusocialmoderation, miyupolls, miyukinibb, miyukini-central, miyukini-service-ui, miyukini-admin, lord_of_the_castle, miyuclicker, miyuvoicecapture (miyualicia-capture), miyuwakeword (miyualicia-wakeword)
Note: bonding_brother n'existe pas dans le workspace.
Fichiers audites: lib.rs + Cargo.toml de chaque crate (24 fichiers) + fichiers cibles par grep (unwrap, panic, unsafe, tests)
Debut: 2026-03-02T16:00:00Z
Fin: 2026-03-02T17:30:00Z

## Findings

| # | Sev | Crate | Fichier:Ligne | Description | Recommandation |
|---|-----|-------|---------------|-------------|----------------|
| 1 | MAJOR | miyukini-central | crates/miyukini-central/src/mws/mod.rs:509 | `unwrap()` en production : `self.config.jayxpose_vitrine_base_url.clone().unwrap()`. Bien que protege par un `is_some()` sur la ligne 506, le pattern `if x.is_some() { x.unwrap() }` est fragile : une modification future du guard peut casser le contrat silencieusement. | Remplacer par `if let Some(ref url) = self.config.jayxpose_vitrine_base_url { ... }` ou utiliser `.expect("guarded by is_some check above")` au minimum. Idealement restructurer avec un `match` ou `if let` combinant les deux Options (url + slug). |
| 2 | MAJOR | miyukini-central | crates/miyukini-central/src/mws/mod.rs:510 | `unwrap()` en production : `self.jayxpose_vitrine_slug.clone().unwrap()`. Meme pattern fragile que #1. | Meme recommandation que #1 : restructurer avec un `if let (Some(url), Some(slug))` combine. |
| 3 | MAJOR | miyukini-admin | crates/miyukini-admin/src/main.rs:657 | `unwrap()` en production : `rows.get_mut(idx - 1).unwrap()`. Le guard `idx == 0 || idx > rows.len()` sur les lignes 654-655 protege normalement, mais le pattern `guard + unwrap` est fragile. Un index concurrent (modification entre le check et l'acces) ou un refactoring du guard peut causer un panic en production. | Remplacer par `let Some(row) = rows.get_mut(idx - 1) else { return Json(...) };` (let-else pattern). |
| 4 | MAJOR | lord_of_the_castle | crates/lord_of_the_castle/src/game_state.rs:921 | `unwrap()` en production : `self.equipped.remove(&slot).unwrap()`. Le guard `contains_key(&slot)` sur la ligne 917 protege, mais le pattern `contains_key + remove + unwrap` est fragile et non idiomatique. | Remplacer par `if let Some(old) = self.equipped.remove(&slot) { ... }` qui est atomique et elimine le risque de panic. |
| 5 | MAJOR | lord_of_the_castle | crates/lord_of_the_castle/src/loot.rs:1643 | `unwrap()` en production : `entries.last().unwrap()` dans `pick_weighted_weapon()`. Si `entries` est un slice vide (invariant casse), panic en production. | Ajouter un `debug_assert!(!entries.is_empty())` + remplacer par `.last().unwrap_or(&DEFAULT_ENTRY)` ou retourner `Option<&Entry>`. |
| 6 | MAJOR | lord_of_the_castle | crates/lord_of_the_castle/src/loot.rs:1657 | `unwrap()` en production : `SHIELD_ENTRIES.last().unwrap()`. Meme pattern que #5 sur une constante statique. Bien que la constante ne sera jamais vide (initialisee au compile), `unwrap()` viole la regle zero-unwrap-en-prod. | Remplacer par un `const` assert ou par `.last().expect("SHIELD_ENTRIES is non-empty compile-time constant")` si on accepte les expect documentes, sinon extraire le dernier element en constante separee. |
| 7 | MAJOR | miyuclicker | crates/miyuclicker/src/carte.rs:41 | `unwrap()` en production : `state.cites.iter().find(...).unwrap()`. Le guard `to_exists` (ligne 30-33) protege avec un early return, mais le pattern est fragile. | Remplacer par `let Some(to_cite) = state.cites.iter().find(...) else { return Err(...) };`. |
| 8 | MAJOR | miyuclicker | crates/miyuclicker/src/carte.rs:69 | `unwrap()` en production : `state.route_duree_by_pair.as_ref().unwrap()`. Le guard sur la ligne 66-68 initialise le champ s'il est `None`, mais le pattern `if none { set } then unwrap` est fragile. | Remplacer par `let routes_map = state.route_duree_by_pair.get_or_insert_with(|| build_route_duree_map(&state.routes));`. |
| 9 | MAJOR | miyupolls | crates/miyupolls/src/ (entier crate) | Aucun test unitaire dans le crate miyupolls (5 fichiers .rs, 4 modules publics : poll, admin_cell, context, errors). L'API publique expose `poll_create`, `poll_vote`, `poll_list`, `poll_result` sans aucune couverture. | Ajouter des tests unitaires dans `lib.rs` ou un module `tests.rs` couvrant au minimum : creation de sondage, vote, comptage des resultats. Priorite haute car c'est une API pub. |
| 10 | MAJOR | miyukinibb | crates/miyukinibb/src/ (entier crate) | Aucun test unitaire dans le crate miyukinibb (4 fichiers, 3 modules publics : client, errors, types). L'API publique expose `MiyukiniBbClient::new()` et `sync_profile()` sans aucune couverture. | Ajouter des tests unitaires couvrant : construction du client (URL valide/invalide), validation du profil (champs vides), serialisation du `ForumProfileSync`. Le `sync_profile()` necessite un mock HTTP pour tester le happy path. |
| 11 | MAJOR | lord_of_the_castle | crates/lord_of_the_castle/src/ (entier crate) | Aucun test unitaire dans le crate lord_of_the_castle (30 fichiers .rs, 16 modules publics, ~3000+ lignes). API publique riche (game_state, loot, save, enemies, player, towers, troops, combat, character_creation) sans aucune couverture. | Priorite critique. Ajouter des tests pour au minimum : `GameState` (creation, transitions de phase), `equip_item()` (avec les unwrap identifies), save/load DB `:memory:`, loot tables. |
| 12 | MAJOR | miyuclicker | crates/miyuclicker/src/ (entier crate) | Aucun test unitaire dans le crate miyuclicker (6 fichiers .rs, 5 modules publics : carte, combat, idlesim, save, state). API publique expose gameplay complet (idle sim, combat, carte strategique, sauvegarde) sans aucune couverture. | Ajouter des tests couvrant au minimum : `GameState` creation, tick idlesim, combat resolve, move_troops (avec les unwrap identifies). |
| 13 | MAJOR | miyukini-service-ui | crates/miyukini-service-ui/src/ (entier crate) | Aucun test unitaire dans le crate miyukini-service-ui (3 fichiers .rs). Bien que ce soit une facade de retrocompatibilite, les hooks `use_theme()` et `use_palette()` sont utilises par 8 apps standalone. | Ajouter au minimum un test verifiant que `ThemeContext` se construit correctement et que les re-exports sont accessibles. |
| 14 | MINOR | miyukinibb | crates/miyukinibb/Cargo.toml:24 | Clippy pedantic absent : seul `all = { level = "warn", priority = -1 }` est configure. La convention projet exige aussi `pedantic = { level = "warn", priority = -1 }`. | Ajouter `pedantic = { level = "warn", priority = -1 }` et les allows standards du workspace, ou passer a `[lints] workspace = true`. |
| 15 | MINOR | miyukinibb | crates/miyukinibb/src/ (3 fichiers) | Aucune annotation MSCM (@id, @do, @role, @layer, @human) dans le crate. La convention projet exige ces annotations sur tout nouveau module. | Ajouter les annotations MSCM sur `lib.rs`, `client.rs`, `types.rs`, `errors.rs`. |
| 16 | MINOR | miyukini-service-ui | crates/miyukini-service-ui/src/ (3 fichiers) | Aucune annotation MSCM (@id, @do, @role, @layer, @human) dans le crate. | Ajouter les annotations MSCM sur `lib.rs`, `theme.rs`, `styles.rs`. |
| 17 | MINOR | lord_of_the_castle | crates/lord_of_the_castle/Cargo.toml:38 | `missing_docs = "allow"` : la convention projet prefere `missing_docs = "warn"`. Avec 30 fichiers et 16 modules publics, le crate gagnerait a documenter ses API publiques. | Changer en `missing_docs = "warn"` et ajouter les doc comments manquants progressivement. |
| 18 | MINOR | miyuclicker | crates/miyuclicker/Cargo.toml:17 | `missing_docs = "allow"` : meme situation que lord_of_the_castle. | Changer en `missing_docs = "warn"`. |
| 19 | INFO | miyusocialprofile | crates/miyusocialprofile/src/lib.rs | 2 tests presents (profile_and_follow, no_mandate_refused). Annotations MSCM presentes. `unsafe_code = "forbid"` conforme. Pedantic conforme. Pas de unwrap en production. | -- |
| 20 | INFO | miyusocialmessaging | crates/miyusocialmessaging/src/lib.rs | 2 tests presents (conversation_and_dm, no_mandate_refused). Annotations MSCM presentes. `unsafe_code = "forbid"` conforme. Pedantic conforme. Pas de unwrap en production. | -- |
| 21 | INFO | miyusocialmoderation | crates/miyusocialmoderation/src/lib.rs | 2 tests presents (post_report_block, no_mandate_refused). Annotations MSCM presentes. `unsafe_code = "forbid"` conforme. Pedantic conforme. Pas de unwrap en production. | -- |
| 22 | INFO | miyuvoicecapture | crates/miyuvoicecapture/src/lib.rs | Tests presents dans vad.rs (dans bloc `#[cfg(test)]`). Annotations MSCM presentes. `#![forbid(unsafe_code)]` en attribut inline + `unsafe_code = "forbid"` dans Cargo.toml (double protection). Pedantic conforme. | -- |
| 23 | INFO | miyuwakeword | crates/miyuwakeword/src/lib.rs | 7 tests presents (admin_cell_creation, admin_cell_singleton, governed_context, toolkit_id_format, error_display, model_type_validation). Annotations MSCM presentes. `#![forbid(unsafe_code)]` inline + Cargo.toml. Pedantic conforme. | -- |
| 24 | INFO | miyukini-central | crates/miyukini-central/src/ | Tests presents dans config.rs, auth/password.rs, db/kindmother_adapter.rs, mws/mod.rs (~15 tests). `unsafe_code = "forbid"` conforme. Pedantic conforme. Annotations MSCM partielles (certains modules annotes, d'autres non). | -- |
| 25 | INFO | miyukini-admin | crates/miyukini-admin/src/ | Tests presents dans 10 modules (api_routes, api_handlers, config, bonding_brother_bridge, database_service, audit_logger, monitoring_service, testing_service, migration_service, security_service, backup_service). Annotations MSCM presentes et exhaustives. `unsafe_code = "forbid"` conforme. Pedantic conforme. | -- |

## Resume
- CRITICAL: 0, MAJOR: 13, MINOR: 5, INFO: 7

## Synthese par axe

### unwrap()/expect()/panic! hors tests
- 8 `unwrap()` en production identifies (findings #1-#8) repartis sur 4 crates :
  - `miyukini-central` : 2 occurrences (mws/mod.rs:509,510)
  - `miyukini-admin` : 1 occurrence (main.rs:657)
  - `lord_of_the_castle` : 3 occurrences (game_state.rs:921, loot.rs:1643, loot.rs:1657)
  - `miyuclicker` : 2 occurrences (carte.rs:41, carte.rs:69)
- Pattern recurrent : guard condition (is_some/contains_key/any) suivi d'un unwrap separe. Non idiomatique Rust, fragile face aux refactorings.
- 0 `panic!` en production (les 4 occurrences dans miyuwakeword/detector.rs et models.rs sont dans des blocs `#[test]`)
- 0 `unsafe` dans tous les crates audites
- 0 `expect()` en production dans ce lot

### Couverture tests
- **Avec tests** (conforme) : miyusocialprofile (2), miyusocialmessaging (2), miyusocialmoderation (2), miyuwakeword (7), miyuvoicecapture (tests VAD), miyukini-central (~15), miyukini-admin (~15)
- **Sans aucun test** (non conforme, MAJOR) : miyupolls, miyukinibb, lord_of_the_castle, miyuclicker, miyukini-service-ui
- Les 5 crates sans tests representent une surface d'API publique significative, en particulier lord_of_the_castle (30 fichiers, 16 modules) et miyuclicker (6 fichiers, 5 modules).

### Cargo.toml conformite
- `unsafe_code = "forbid"` : present dans les 12 crates (conforme a 100%)
- `pedantic` lint : conforme dans 11/12 crates. miyukinibb n'a que `all = "warn"` sans `pedantic` (finding #14)
- `missing_docs` : "allow" dans lord_of_the_castle et miyuclicker (MINOR), "warn" dans la plupart des autres

### Annotations MSCM
- Presentes et coherentes : miyusocialprofile, miyusocialmessaging, miyusocialmoderation, miyupolls, miyuvoicecapture, miyuwakeword, miyukini-admin, miyuclicker
- Absentes : miyukinibb (finding #15), miyukini-service-ui (finding #16)
- Partielles : miyukini-central, lord_of_the_castle (lib.rs annote mais pas tous les sous-modules)

### Gestion d'erreur
- Tous les crates utilisent des types d'erreur explicites (pattern conforme) : `MiyusocialprofileError`, `MiyusocialmessagingError`, `MiyusocialmoderationError`, `MiyupollsError`, `MiyukiniBbError`, `SaveError`, `VoiceCaptureError`, `WakeWordError`
- miyukinibb utilise `thiserror` proprement avec variants typees
- Les toolkits social utilisent des erreurs custom via le kernel

### Crates absents
- `bonding_brother` : n'existe pas dans le workspace (le module `bonding_brother_bridge` dans miyukini-admin reference le crate `bondingbrother` qui est un crate Core, pas un toolkit/service)

## Erreurs rencontrees
Aucune. Tous les fichiers ont ete lus avec succes.
