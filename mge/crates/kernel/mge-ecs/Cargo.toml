# @id: MGE-ECS @do: kernel @role: back-end @layer: 1 @human: miyuk
# mge-ecs — MGE ECS: archetype SoA storage, queries, commands

[package]
name = "mge-ecs"
version.workspace = true
edition.workspace = true
description = "MGE ECS: archetype SoA storage, queries, commands"

[dependencies]
tracing = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
tracing-subscriber = { workspace = true }

[lints]
workspace = true
