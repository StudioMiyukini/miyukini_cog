[package]
name = "mge-core"
version = "0.1.0"
edition = "2021"
description = "MGE Kernel — Engine, boot, tick, plugin trait, config"

[dependencies]
mge-ecs = { path = "../mge-ecs" }
mge-event = { path = "../mge-event" }
mge-profiler = { path = "../mge-profiler" }
mge-rng = { path = "../mge-rng" }
mge-time = { path = "../mge-time" }

[dev-dependencies]
rand = { version = "0.8", features = ["small_rng"] }

[lints.rust]
unsafe_code = "forbid"
