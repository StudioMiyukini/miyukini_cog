# Finding List P2-L2a-D1-01
Agent: Francois
Domaine: D1 -- Sante code
Lot: L2 -- Sous-lot: L2a (jayxpose)
Crates: jayxpose (crate), jayxpose-app (app)
Fichiers audites: 23/23 (.rs) + 2 Cargo.toml
Debut: 2026-03-02T14:00:00Z
Fin: 2026-03-02T14:45:00Z

## Findings

| # | Sev | Fichier:Ligne | Description | Recommandation |
|---|-----|---------------|-------------|----------------|
| 1 | MAJOR | crates/jayxpose/src/screens/exp/e07_vitrine_presentation.rs:2002 | `expect("props should be object")` en production dans `ensure_props_object()`. Panic si la branche `json!({})` echoue (theoriquement impossible mais viole la regle zero-expect-en-prod). | Remplacer par `.as_object_mut().unwrap_or_else(\|\| unreachable!())` ou retourner `Result`. Mieux : renvoyer un `&mut Map` via pattern match sans expect. |
| 2 | MAJOR | crates/jayxpose/src/data/kindmother_db.rs:63 | `path.parent().unwrap_or(path)` dans le bloc `#[cfg(feature = "db-encryption")]`. Si `path` est vide ou racine, `parent()` retourne `None` et `unwrap_or` masque silencieusement l'erreur. Contexte : derivation de cle de chiffrement, chemin critique. | Remonter une `DbError` explicite si `parent()` est `None` plutot que fallback silencieux. |
| 3 | MAJOR | crates/jayxpose/src/data/kindmother_db.rs:67 | `.unwrap_or("jayxpose")` dans le bloc `#[cfg(feature = "db-encryption")]`. Si le fichier n'a pas de stem, on derive une cle pour "jayxpose" au lieu du vrai nom, ce qui pourrait ouvrir une mauvaise DB ou corrompre la cle. | Remonter une `DbError` si `file_stem()` retourne `None`. |
| 4 | MAJOR | crates/jayxpose/Cargo.toml:47 | `pedantic = "allow"` desactive toute l'analyse clippy pedantic. La convention projet exige `pedantic = { level = "warn", priority = -1 }`. | Changer en `pedantic = { level = "warn", priority = -1 }` et traiter les warnings generes. |
| 5 | MAJOR | crates/jayxpose/src/data/kindmother_db.rs (52 occurrences) | `self.conn.lock().map_err(...)` sur les 52 methodes. Un `Mutex::lock()` panique si le mutex est empoisonne (un thread a panique pendant le lock). Le `map_err` ne protege pas contre le poison. | Pattern : `.lock().unwrap_or_else(\|e\| e.into_inner())` pour ignorer le poison, ou utiliser `parking_lot::Mutex` qui ne s'empoisonne pas, ou documenter la strategie de poison. |
| 6 | MAJOR | crates/jayxpose/src/data/kindmother_db.rs | Aucun test unitaire dans le module DB legacy-sqlite (1637 lignes, 0 `#[test]`). C'est la couche de persistance principale (CRUD complet, 16 tables, 52 methodes publiques). | Ajouter des tests `:memory:` couvrant au minimum : open, init_schema, exposant CRUD, produit CRUD, document CRUD. Priorite haute. |
| 7 | MAJOR | crates/jayxpose/src/data/kindmother_client_db.rs | Aucun test unitaire dans le module DB kindmother-client (550 lignes, 0 `#[test]`). | Ajouter des tests avec mock du `KindMotherClient` ou tests d'integration. |
| 8 | MAJOR | crates/jayxpose/src/governance.rs:63 | `let _ = db.sync_log_insert(...)` : le resultat est silencieusement ignore. Si l'insertion echoue, l'audit trail est perdu sans aucune trace. | Propager l'erreur avec `?` ou au minimum logger l'echec avec `tracing::warn!`. |
| 9 | MAJOR | apps/jayxpose/ (0 tests) | Aucun test dans l'app jayxpose-app (13 fichiers, 0 `#[test]`). L'app entiere n'a aucune couverture. | Ajouter au minimum des tests d'integration pour la construction des contextes et le routing des vues. |
| 10 | MAJOR | crates/jayxpose/src/screens/exp/mod.rs (entier fichier) | Module `screens/exp/` utilise `eframe::egui` (ancien framework UI eGUI). Le Cargo.toml ne declare aucune dependance `eframe`/`egui`. Ce code est probablement mort (non compile) suite a la migration vers Dioxus/Tauri. | Confirmer que ce module est mort et le supprimer. S'il est encore compile, ajouter la dependance manquante. |
| 11 | MINOR | crates/jayxpose/src/data/types.rs:530,576,614,651,775,812,866 | 7 methodes `pub fn from_str()` avec `#[allow(clippy::should_implement_trait)]`. Ce sont des conversions string->enum qui devraient implementer `std::str::FromStr` au lieu de shadower le trait standard. | Implementer `FromStr` pour chaque enum (avec `type Err = String` ou un type d'erreur dedie). Supprimer les `#[allow]`. |
| 12 | MINOR | apps/jayxpose/Cargo.toml:27-28 | `[lints] workspace = true` : les lints sont herites du workspace. OK, mais le workspace Cargo.toml devrait etre verifie pour confirmer `unsafe_code = "forbid"` et `pedantic = "warn"`. | Verifier que le workspace root definit les lints corrects. |
| 13 | MINOR | crates/jayxpose/src/auth/mod.rs:73 | `let user_id = profile.id.clone().unwrap_or_default()` : si `id` est `None`, le `user_id` est une chaine vide, ce qui cree une session invalide (access_token vide). | Retourner `AuthError` si `profile.id` est `None`. |
| 14 | MINOR | crates/jayxpose/src/auth/mod.rs:44-50 | Hashage mot de passe avec SHA-256 brut, sans sel. Vulnerable aux rainbow tables et attaques par dictionnaire. | Migrer vers Argon2id ou bcrypt avec sel unique par utilisateur. Le projet utilise deja Argon2id dans MiyuCloud. |
| 15 | MINOR | crates/jayxpose/src/data/types.rs | Structs domaine : tous les champs sont `Option<String>`. Pas de validation de format (email, SIRET, slug). | Ajouter une methode `validate()` sur `ExposantProfile` et `ProduitCatalogue` pour verifier les invariants metier avant persistance. |
| 16 | MINOR | apps/jayxpose/src/views/mod.rs:64 | `db.exposants_list_annuaire().unwrap_or_default()` appele a chaque rendu du composant racine. Requete DB a chaque frame Dioxus. | Cacher le resultat dans un `use_resource` ou `use_signal` avec chargement au montage uniquement. |
| 17 | INFO | crates/jayxpose/src/auth/mod.rs | 7 tests unitaires presents (auth_session_construction, auth_session_with_profile, auth_error_display, auth_sign_out_ok, hash_password_deterministic, hash_password_different_inputs, hash_password_not_plaintext). Couverture correcte du module auth. | -- |
| 18 | INFO | crates/jayxpose/Cargo.toml:41-42 | `unsafe_code = "forbid"` present. Conforme a la regle projet. | -- |
| 19 | INFO | apps/jayxpose/Cargo.toml:27-28 | `[lints] workspace = true` herite les lints workspace. | -- |
| 20 | INFO | crates/jayxpose/src/data/kindmother_db.rs | Gestion d'erreur coherente : `DbError(String)` + `From<rusqlite::Error>`, toutes les methodes retournent `Result<T, DbError>`. Pas de `unwrap()` en production dans ce fichier (les 2 mentionnes sont dans le bloc `#[cfg(feature = "db-encryption")]`). | -- |
| 21 | INFO | crates/jayxpose/ (lib.rs, data/mod.rs, types.rs, governance.rs) | Annotations MSCM presentes et coherentes (@id, @do, @layer). | -- |
| 22 | INFO | crates/jayxpose/src/data/kindmother_client_db.rs | Utilise `thiserror` pour les types d'erreur (bon pattern). Gestion d'erreur propre avec `DbError::Client`, `DbError::Serialization`, `DbError::Internal`, `DbError::NotInitialized`. | -- |

## Resume
- CRITICAL: 0, MAJOR: 9, MINOR: 6, INFO: 6

## Synthese par axe

### unwrap()/expect()/panic! hors tests
- 1 `expect()` en production (finding #1, e07_vitrine_presentation.rs:2002)
- 2 `unwrap_or()` dans un contexte critique crypto (findings #2 et #3, kindmother_db.rs:63,67)
- 0 `panic!`, 0 `unreachable!`, 0 `unwrap()` hors tests
- Le `unwrap()` dans auth/mod.rs:158 est dans un bloc `#[cfg(test)]` : conforme.

### Couverture tests
- 7 tests dans `crates/jayxpose/src/auth/mod.rs` (module auth)
- 0 tests dans `crates/jayxpose/src/data/kindmother_db.rs` (1637 lignes, 52 methodes pub)
- 0 tests dans `crates/jayxpose/src/data/kindmother_client_db.rs` (550 lignes)
- 0 tests dans `crates/jayxpose/src/governance.rs`
- 0 tests dans `apps/jayxpose/` (13 fichiers)
- **Total : 7 tests pour l'ensemble jayxpose. Ratio : ~0.3 test / 100 lignes.**

### Cargo.toml
- `unsafe_code = "forbid"` : present dans le crate (conforme)
- App herite du workspace (`[lints] workspace = true`)
- `pedantic = "allow"` dans le crate : non conforme (devrait etre "warn")

### Gestion d'erreur
- Module DB legacy-sqlite : `DbError(String)` custom, coherent, pas de unwrap en production
- Module DB kindmother-client : `thiserror` avec variants typos, propre
- Module auth : `AuthError` custom, propre
- Module governance : resultat d'audit silencieusement ignore (finding #8)

### Code potentiellement mort
- `crates/jayxpose/src/screens/exp/` : 3+ fichiers utilisant `eframe::egui` qui n'est pas declare en dependance. Probable code mort post-migration Dioxus.

## Erreurs rencontrees
Aucune. Tous les fichiers ont ete lus avec succes (certains fichiers longs ont necessite une lecture paginee).
