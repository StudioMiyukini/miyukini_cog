[package]
name = "mge-rpg-stats"
version = "0.1.0"
edition = "2021"
description = "MGE rpg â€” stats module"

[features]
serde = ["dep:serde"]

[dependencies]
mge-core = { path = "../../mge-core" }
mge-ecs = { path = "../../mge-ecs" }
mge-event = { path = "../../mge-event" }
serde = { version = "1", features = ["derive"], optional = true }

[lints.rust]
unsafe_code = "forbid"
