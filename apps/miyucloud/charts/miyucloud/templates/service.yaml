---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "miyucloud.fullname" . }}
  labels:
    {{- include "miyucloud.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "miyucloud.selectorLabels" . | nindent 4 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "miyucloud.fullname" . }}-headless
  labels:
    {{- include "miyucloud.labels" . | nindent 4 }}
spec:
  clusterIP: None
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "miyucloud.selectorLabels" . | nindent 4 }}