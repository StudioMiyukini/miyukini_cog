{
  "name": "miyukini-cog",
  "private": true,
  "version": "0.1.0",
  "description": "Miyukini COG — Core-Orchestrated Governance Environment",
  "scripts": {
    "dev": "pnpm -F miyukini-central dev",
    "build": "pnpm -F miyukini-central build",
    "tauri:dev": "pnpm -F miyukini-central tauri dev",
    "tauri:build": "pnpm -F miyukini-central tauri build",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r exec rm -rf node_modules dist .turbo"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "packageManager": "pnpm@9.0.0"
}
