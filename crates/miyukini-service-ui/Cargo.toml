# miyukini-service-ui — Fondations UI partagées entre tous les services standalone.
# Thème, palette, styles, hooks Dioxus communs.

[package]
name = "miyukini-service-ui"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Fondations UI Miyukini — thème, palette, styles partagés entre services"

[dependencies]
dioxus = { version = "0.6", features = ["desktop"] }

[lints]
workspace = true
