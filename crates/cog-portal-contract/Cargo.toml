# @id: cog_portal_contract_cargo @do: declare_portal_contract_crate @role: api @layer: service
# @human: Crate contrat d'exposition Portal — définit PortalContract trait + types PublicPage/PortalError.
# Shared par cog-web-portal et services Jay pour typage commun.

[package]
name = "cog-portal-contract"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "COG Portal Contract — PortalContract trait + PublicPage + PortalError"

[dependencies]
thiserror = "2"
serde = { version = "1", features = ["derive"] }

[lints]
workspace = true
