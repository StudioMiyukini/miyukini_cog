[package]
name = "miyukini-spm-cms-publication"
version = "0.1.0"
edition = "2021"
description = "Module Publication du SPM CMS — Gestion du cycle éditorial générique des entités de contenu"

[dependencies]
miyukini-kernel = { path = "../miyukini-kernel" }

[features]
default = []
memory = []  # Implémentation en mémoire pour tests/démo
