[package]
name = "miyumarket"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Miyukini Service Market — Protocole partagé Central/Origin pour l'installation et la publication de services"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = { version = "0.4", features = ["serde"] }
sha2 = "0.10"
thiserror = "2"

[lints]
workspace = true
