# @id: MGE-Rescale @do: tool @role: back-end @layer: 6 @human: miyuk
# mge-rescale — MGE CLI: batch image rescaler (nearest-neighbor for pixel art)

[package]
name = "mge-rescale"
version.workspace = true
edition.workspace = true
description = "MGE CLI: batch image rescaler (nearest-neighbor for pixel art)"

[dependencies]
image = { workspace = true }
serde = { workspace = true }
toml = { workspace = true }
thiserror = { workspace = true }

[lints]
workspace = true
