# @id: MGE-Core @do: kernel @role: back-end @layer: 1 @human: miyuk
# mge-core — MGE kernel: game loop, time, events, system scheduler

[package]
name = "mge-core"
version.workspace = true
edition.workspace = true
description = "MGE kernel: game loop, time, events, system scheduler"

[dependencies]
tracing = { workspace = true }

[dev-dependencies]
tracing-subscriber = { workspace = true }

[lints]
workspace = true
