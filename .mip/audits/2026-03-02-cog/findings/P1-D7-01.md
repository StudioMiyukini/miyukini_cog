<!--
@id: hugo_audit_infra_p1_d7_01
@do: scan_infrastructure_cog_workspace
@role: devops_infra_audit
@layer: audit
@human: Scan automatise infrastructure DevOps du workspace COG (hors mge/). Hugo. 2026-03-02.
-->

# P1-D7-01 — Scan Infrastructure COG

**Agent** : Hugo (DevOps & Infrastructure)
**Debut** : 2026-03-02T10:00:00
**Fin** : 2026-03-02T10:18:00
**Perimetre** : workspace `Miyukini-COG/` — EXCLUS : `mge/`, `target/`
**Methode** : grep automatise, inspection fichiers, analyse gitignore/git-tracked

---

## Synthese executive

| Categorie | Findings | CRITICAL | MAJOR | MINOR |
|-----------|----------|----------|-------|-------|
| CI/CD | 2 | 0 | 2 | 0 |
| Docker | 1 | 0 | 0 | 1 |
| Secrets | 3 | 2 | 1 | 0 |
| Config prod/dev | 2 | 0 | 1 | 1 |
| Ports | 1 | 0 | 0 | 1 |
| Health checks | 2 | 0 | 1 | 1 |
| Logging | 2 | 0 | 0 | 2 |
| TLS | 2 | 0 | 1 | 1 |
| Backup | 2 | 0 | 1 | 1 |
| Reproductibilite | 1 | 0 | 0 | 1 |
| **TOTAL** | **18** | **2** | **7** | **9** |

---

## Finding List

| # | Sev | Categorie | Constat | Impact | Recommandation |
|---|-----|-----------|---------|--------|----------------|
| F-01 | CRITICAL | Secrets | `Origin_VPS_SSH_and_IP` commite dans git (git-tracked). Contient l'IP publique du VPS (46.202.129.65) et la cle publique SSH ed25519. | Exposition de l'IP et empreinte SSH du serveur de production dans l'historique git public potentiel. | Supprimer du tracking git (`git rm --cached Origin_VPS_SSH_and_IP`), ajouter au `.gitignore`, faire un `git filter-branch` ou `git-filter-repo` si l'historique est deja pousse. Stocker dans un gestionnaire de secrets (Vault, 1Password). |
| F-02 | CRITICAL | Secrets | `scripts/setup-origin-hostinger.sh` commite dans git. Contient l'IP de production hardcodee (`ORIGIN_IP="46.202.129.65"`). | IP de production exposeee dans le depot source. | Parametriser l'IP via variable d'environnement ou argument CLI. Supprimer la valeur hardcodee du script. |
| F-03 | MAJOR | Secrets | `apps/miyucloud/src/config.rs` ligne 64 : `MIYUCLOUD_OWNER_ID` a un fallback hardcode `"default-owner"`. Sans cette variable d'env, le owner ID est previsible en production. | Privilege escalation potentiel si un attaquant connait l'ID par defaut. | Generer un UUID v4 aleatoire au premier lancement (pattern identique a `generate_random_token()` deja present) ou exiger la variable d'env obligatoire au demarrage. |
| F-04 | MAJOR | CI/CD | Aucun pipeline CI/CD detecte. Ni `.github/workflows/`, ni `.gitlab-ci.yml`, ni `Jenkinsfile`, ni `.circleci/`. | Aucune integration continue : pas de lint automatique, pas de tests automatiques sur PR, pas de detection de regression, pas de cargo audit. DORA metric "deployment frequency" non mesurable. | Creer `.github/workflows/ci.yml` avec les stages : lint (clippy), test (cargo test), security (cargo audit), build release. |
| F-05 | MAJOR | CI/CD | Aucun `cargo audit` ni `cargo deny` configure dans le workspace. Aucun fichier `deny.toml` detecte. | Vulnerabilites CVE dans les dependances non detectees automatiquement. Risque RUSTSEC. | Ajouter `cargo audit` et `cargo deny` dans le pipeline CI/CD. Creer `deny.toml` a la racine du workspace. |
| F-06 | MAJOR | Health checks | Les apps desktop Dioxus (`central`, `jay1tribu`, `jayfestival`, `jaykoa`, `jaykonta`, `jaymanga`, `jayxpose`, `miyuclicker`, `miyukiniwatch`) n'exposent aucun endpoint `/health`. Ce sont des apps Dioxus desktop — pas de serveur HTTP. | Aucune supervision possible. Pas de health check pour le ServiceManager de Central qui lance ces services. | Ajouter un ping IPC minimal ou un port TCP local par service permettant de verifier la vivacite. Central ServiceManager peut verifier l'exit code du processus comme fallback. |
| F-07 | MAJOR | Config prod/dev | Tous les binaires desktop (10 apps) ont le niveau de log `=debug` hardcode dans le code source (ex. `"miyukini_central=debug,kindmother_service=info,dioxus=info"`). Ce n'est pas configurable sans recompiler. | En production, le log debug peut exposer des donnees internes et degrader les performances. Violation de la separation dev/prod. | Remplacer les filtres hardcodes par `EnvFilter::try_from_default_env().unwrap_or_else(|_| EnvFilter::new("info"))` pour permettre la configuration via `RUST_LOG`. |
| F-08 | MAJOR | TLS | Le certificat TLS de `apps/origin` (expose sur VPS) est auto-signe (openssl self-signed, 365 jours) dans le script d'installation. Certbot est installe mais la generation Let's Encrypt n'est pas automatisee. | Avertissements de securite navigateur. Pas de renouvellement automatique. Expiration au bout de 365 jours sans alerte. | Integrer la generation Let's Encrypt via certbot dans le workflow de deploiement. Ajouter un cron de renouvellement (`certbot renew`). Documenter la procedure. |
| F-09 | MAJOR | Backup | La crate `miyukini-admin` contient un `BackupService` trait mais aucune implementation de sauvegarde automatique planifiee n'est detectee dans les scripts de deploiement. Les repertoires backup existent (`/var/lib/miyukini/backup`) mais aucun cron de backup n'est configure dans `setup-origin.sh`. | Risque de perte totale de donnees KindMother en cas de defaillance VPS. | Implementer un cron systemd timer (`miyukini-backup.timer`) qui declenche une sauvegarde SQLite quotidienne vers `/var/lib/miyukini/backup/` avec rotation (7 jours). Documenter la procedure de restauration. |
| F-10 | MINOR | Docker | Aucun `Dockerfile`, `docker-compose.yml` ni `.dockerignore` detecte dans le workspace. | Pas de conteneurisation. Deploiement uniquement possible sur un environnement avec Rust installe. Pas d'isolation des services. | Pour `apps/origin` (service VPS) et `apps/miyucloud` (serveur dual), creer des Dockerfiles multi-stage (builder rust:alpine + runtime debian-slim). Ajouter docker-compose.yml pour orchestration locale dev. |
| F-11 | MINOR | Config prod/dev | Aucune separation explicite de configuration prod/dev (pas de `config.prod.toml`, pas de feature flags `dev`/`prod`, pas de profils Cargo distincts pour la configuration runtime). `apps/origin` a un mecanisme de configuration par fichier TOML mais sans profil nomme. | Risque de deployer une configuration de developpement en production. | Formaliser les profiles : `config.dev.toml` et `config.prod.toml` avec selection via variable d'environnement `APP_ENV=prod`. |
| F-12 | MINOR | Ports | Ports hardcodes directement dans le code source (non exhaustif) : `50051` (KindMother gRPC), `11435` (miou-llm-bridge), `11440` (MiyuCloud API), `11442` (MiyuCloud Web TLS), `8090` (MWS), `7000` (Origin Relay), `21000` (Origin Tracker), `12345`/`12346` (tests pool tracker). | Collision de ports possible en environnement multi-instances. Documentation implicite uniquement. | Centraliser l'inventaire des ports dans un fichier `PORTS.md` ou dans `.mip/environment.md`. S'assurer que tous les ports productifs sont configurables via env vars (pattern deja applique dans `miyucloud/config.rs`). |
| F-13 | MINOR | Health checks | `apps/origin` expose `/api/health` et `/api/status` (detectes dans `web/server.rs` et `web/api.rs`). Cependant, ces endpoints ne font pas de verification de la disponibilite de la base de donnees KindMother ni du tracker. Retournent `"status": "healthy"` de maniere statique. | Health check superficiel — ne detecte pas une defaillance DB. | Enrichir le handler `/api/health` avec une verification de connectivite KindMother (ping gRPC) et du tracker. Retourner HTTP 503 si un composant critique est indisponible. |
| F-14 | MINOR | Logging | `apps/miyucloud/src/main.rs` ligne 44 utilise `tracing_subscriber::fmt::init()` sans filtre de niveau. Le niveau par defaut est `ERROR` sans `RUST_LOG`. Silencieux en production sans configuration explicite. | Debugging difficile en production si aucune variable `RUST_LOG` n'est definie. | Initialiser avec `EnvFilter::try_from_default_env().unwrap_or_else(|_| EnvFilter::new("miyucloud=info"))`. |
| F-15 | MINOR | Logging | Aucun systeme de log structure (JSON) configure pour les apps serveur (`origin`, `miyucloud`). `tracing_subscriber::fmt()` produit du log texte brut. | Agregation de logs impossible dans un stack ELK/Loki/Grafana sans post-traitement. | Ajouter feature `json` de `tracing-subscriber` et basculer sur format JSON en production via `APP_ENV=prod`. |
| F-16 | MINOR | TLS | `apps/miyucloud` : si `MIYUCLOUD_TLS_CERT` et `MIYUCLOUD_TLS_KEY` ne sont pas fournis, le code bascule sur `TlsConfig::default_paths(base_dir)` qui genere ou charge un certificat auto-signe local. Ce comportement silencieux peut passer inapercu. | Un operateur peut croire que TLS est configure correctement alors qu'il utilise un certificat auto-signe non valide. | Loguer explicitement un avertissement `WARN` au demarrage si les chemins TLS par defaut sont utilises. Documenter la variable d'env obligatoire en production. |
| F-17 | MINOR | Backup | Aucune strategie de retention documentee pour les fichiers de backup SQLite. Le repertoire `/var/lib/miyukini/backup` est cree mais vide dans les scripts. | Sans rotation, les backups remplissent le disque ou n'existent pas. | Definir et documenter la politique de retention (ex. 7 jours quotidien, 4 semaines hebdomadaire). Implémenter la rotation dans le cron de backup. |
| F-18 | MINOR | Reproductibilite | `Cargo.lock` est commite dans git (verifie). Cependant, `.gitignore` contient `Cargo.lock` a la ligne correspondante, ce qui signifie que le lock file est tracked malgre la ligne d'ignorance potentielle. | Possible confusion : si `.gitignore` est mal interprete, des developpeurs pourraient supprimer le lock file. | Verifier que la ligne `Cargo.lock` du `.gitignore` correspond bien a un cas specifique (lib vs bin). Pour un workspace avec binaires, `Cargo.lock` DOIT etre commite. La situation actuelle est correcte mais la ligne d'ignorance cree de la confusion — la supprimer du `.gitignore`. |

---

## Inventaire des ports (reference)

| Port | Service | Bind | Configurable via env |
|------|---------|------|---------------------|
| 50051 | KindMother gRPC | 127.0.0.1 | Oui (`KINDMOTHER_LISTEN_ADDR`) |
| 7000 | Origin Relay (MWS) | 0.0.0.0 | Oui (config TOML) |
| 8090 | MWS Home HTTP | 0.0.0.0 | Partiel (hardcode dans mws_view.rs) |
| 11435 | miou-llm-bridge | 0.0.0.0 | Oui (`MIOU_LISTEN_ADDR` via config) |
| 11440 | MiyuCloud API | 127.0.0.1 | Oui (`MIYUCLOUD_PORT`) |
| 11442 | MiyuCloud Web HTTPS | 0.0.0.0 | Oui (`MIYUCLOUD_WEB_PORT`) |
| 11443 | MiyuCloud UDP peer discovery | 0.0.0.0 | A verifier |
| 21000 | Origin Tracker | 0.0.0.0 | Oui (config TOML) |

---

## Points conformes (reference)

- **Cargo.lock commite** : Builds reproductibles garantis pour les binaires.
- **`unsafe_code = "forbid"`** : Verifie dans CLAUDE.md, applique workspace-wide.
- **`.gitignore` couvre les secrets** : `.env`, `.github-token`, `supabase-catakana.env`, `hostinger_VPS.md`, `hostinger_private_key.*`, `cle_ssh_hostinger.txt` correctement ignorees.
- **MiyuCloud TLS** : `tokio-rustls` + `TlsAcceptor` bien implemente dans `apps/miyucloud/src/main.rs`.
- **MiyuCloud config par env vars** : Pattern propre dans `apps/miyucloud/src/config.rs` — ports, chemins, passphrase tous configurables.
- **miou-llm-bridge** : `/health`, `/status`, `/v1/inference/status` presents.
- **Origin** : `/api/health` et `/api/status` presents.
- **systemd service** : `miyukini-origin.service` configure dans les scripts VPS.
- **firewall UFW** : `scripts/vps-ufw-open-ports.sh` presente avec les ports 22, 80, 443, 7000, 21000.
- **Logging tracing** : Tous les binaires utilisent `tracing_subscriber` (pas de `println!` en prod).

---

## Actions prioritaires recommandees

### Bloquant avant toute mise en production

1. **F-01** : Supprimer `Origin_VPS_SSH_and_IP` du tracking git et purger l'historique.
2. **F-02** : Parametriser l'IP VPS dans `setup-origin-hostinger.sh`.
3. **F-04** : Creer le pipeline CI/CD GitHub Actions minimal.

### Sprint suivant

4. **F-05** : Configurer `cargo audit` + `deny.toml`.
5. **F-07** : Remplacer les log filters hardcodes `=debug` par `RUST_LOG`-driven.
6. **F-08** : Automatiser le renouvellement Let's Encrypt.
7. **F-09** : Implementer le cron de backup KindMother.

### Backlog technique

8. **F-10** : Dockerfiles multi-stage pour `origin` et `miyucloud`.
9. **F-11** : Profiles de configuration prod/dev formalises.
10. **F-13** : Enrichir `/api/health` avec verification DB.

---

*P1-D7-01 — Hugo — 2026-03-02T10:18:00*
