[package]
name = "mge-ecs"
version = "0.1.0"
edition = "2021"
description = "MGE Kernel — World, EntityId, Component trait, SoA storage"

[features]
serde = ["dep:serde"]

[dependencies]
serde = { version = "1", features = ["derive"], optional = true }

[lints.rust]
unsafe_code = "forbid"
