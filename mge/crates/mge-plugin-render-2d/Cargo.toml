[package]
name = "mge-plugin-render-2d"
version = "0.1.0"
edition = "2021"
description = "MGE Core Pack — Sprite, Camera2D, RenderLayer"

[dependencies]
mge-ecs = { path = "../mge-ecs" }
mge-plugin-spatial = { path = "../mge-plugin-spatial" }

[lints.rust]
unsafe_code = "forbid"
